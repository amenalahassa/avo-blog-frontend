
export default {

    SET_CONTENT (state, article) {
        state.content = article
    },

    SET_ABOUT (state, about) {
        state.about = about
    },

    SET_ERROR (state, message) {
        state.error = message
    },

}